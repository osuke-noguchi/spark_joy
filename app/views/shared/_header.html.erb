<div class="container">
  <div class="row">
   <nav class="navbar navbar-light bg-light fixed-top">
      <h5 class="my-0 mr-md-auto font-weight-normal"><%= link_to "Spark Joy", root_path, :style=>"color:black;" %></h5>
    <% if user_signed_in? %>
    <div class="col-md">
     <form class="form-inline mt-2 mt-md-0">
            <input class="form-control mr-sm-2" type="search" placeholder="検索..." aria-label="検索...">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">検索</button>
          </form>
        </div>
         <div class="col-md">
           <%= link_to "アイテム登録", new_item_path, class: "btn btn-outline-primary" %>
　　　　　　 <%= current_user.name %>ログイン中
         </div>
          <nav class="my-2 my-md-0 mr-md-3">
           <a class="p-2 text-dark"><%= link_to 'ログアウト', :destroy_user_session, method: :delete, :style=>"color:black;" %></a>
          </nav>
     <% else %>
      <%= link_to "はじめる", new_user_registration_path, class: "btn btn-outline-primary" %>
      <% end %>

  </nav>
</div>
</div>
